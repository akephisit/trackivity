# API configuration (used at build time and in browser)
# - Leave empty to use same-origin + Vite proxy in dev.
# - Set to external URL to call API directly from the browser.
#   Example: https://trackivity-32t8l.ondigitalocean.app
PUBLIC_API_URL=

# App
NODE_ENV=development
VITE_APP_NAME=Trackivity
VITE_APP_VERSION=1.0.0
