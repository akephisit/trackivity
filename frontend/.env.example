# API configuration (used at build time)
# When building the Docker image, you can override these via:
#   docker build -t trackivity-frontend \
#     --build-arg PUBLIC_API_URL=https://api.example.com \
#     ./frontend
PUBLIC_API_URL=http://localhost:3000

# App
NODE_ENV=production
VITE_APP_NAME=Trackivity
VITE_APP_VERSION=1.0.0
